/// Centralized brand copy shared across screens (e.g. tagline reused on
/// the login hero and, later, a splash screen).
class AppStrings {
  const AppStrings._();

  static const String tagline = 'Conectando pessoas, resolvendo problemas.';
}
